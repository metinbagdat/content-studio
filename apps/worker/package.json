{
  "name": "@content-studio/worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx --env-file=../../.env src/index.ts --drain",
    "start": "tsx --env-file=../../.env src/index.ts --drain",
    "drain": "tsx --env-file=../../.env src/index.ts --drain",
    "loop": "tsx --env-file=../../.env src/index.ts",
    "typecheck": "tsc --noEmit -p ../../tsconfig.json"
  },
  "dependencies": {
    "@content-studio/db": "*"
  }
}
