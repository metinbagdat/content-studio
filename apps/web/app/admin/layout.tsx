import { AdminWorkflowChrome } from '@/components/admin/AdminWorkflowChrome'

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <AdminWorkflowChrome>{children}</AdminWorkflowChrome>
}
